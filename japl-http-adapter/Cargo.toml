[package]
name = "japl-http-adapter"
version = "0.1.0"
edition = "2021"

[dependencies]
wit-bindgen = { version = "0.41", default-features = false, features = ["realloc", "macros"] }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
strip = true
