import Option.{Some, None, map_option, unwrap_or, is_some}

fn double(x: Int) -> Int { x * 2 }

fn main() {
  let a = Some(21)
  let b = None
  println(show(unwrap_or(map_option(a, double), 0)))
  println(show(unwrap_or(b, 99)))
  println(show(is_some(a)))
  println(show(is_some(b)))
}
