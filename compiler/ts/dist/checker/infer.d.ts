import * as AST from '../parser/ast.js';
import { Type, TypeScheme, EffectRow } from './types.js';
import { UnificationEngine } from './unify.js';
import { TypeEnv } from './env.js';
import { TypeError } from './errors.js';
export type TypedModule = {
    decls: AST.Decl[];
    types: Map<AST.Expr, Type>;
    errors: TypeError[];
};
export declare class TypeChecker {
    private unifier;
    private env;
    private effectChecker;
    private errors;
    private exprTypes;
    constructor();
    checkModule(mod: AST.Module): TypedModule;
    getErrors(): TypeError[];
    getEnv(): TypeEnv;
    getUnifier(): UnificationEngine;
    private inferDecl;
    private inferFnDecl;
    private inferTypeDecl;
    private inferRecordTypeDecl;
    inferExpr(expr: AST.Expr): [Type, EffectRow];
    private inferExprInner;
    private inferVar;
    private inferConstructor;
    private inferApp;
    private inferLambda;
    private inferLet;
    private inferMatch;
    private checkExhaustiveness;
    private inferPattern;
    private inferIf;
    private inferPipe;
    private inferBinOp;
    private inferUnaryOp;
    private inferRecord;
    private inferFieldAccess;
    private inferRecordUpdate;
    private inferList;
    private inferBlock;
    private inferSpawn;
    private inferSend;
    private inferReceive;
    private inferTry;
    private inferReturn;
    lowerTypeExpr(te: AST.TypeExpr): Type;
    private lowerTypeExprWithParams;
    private lowerEffectExpr;
    instantiate(scheme: TypeScheme): Type;
    private instantiateType;
    private substituteType;
    generalize(type: Type): TypeScheme;
    mergeEffects(a: EffectRow, b: EffectRow): EffectRow;
}
