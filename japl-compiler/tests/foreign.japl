foreign "japl" fn time_now() -> Int
foreign "japl" fn tcp_listen(port: Int) -> Int
foreign "japl" fn tcp_close(id: Int)

fn main() {
  let t = time_now()
  println(show(t))
  let listener = tcp_listen(0)
  tcp_close(listener)
  println("TCP works")
}
