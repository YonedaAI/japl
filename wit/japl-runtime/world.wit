package japl:runtime@0.1.0;

/// Process interface is defined in processes.wit.
/// It provides: spawn, send, receive, self-pid, mailbox-size, is-alive.

interface logging {
    /// Print a line to stdout.
    /// Stability: EXPERIMENTAL
    println: func(message: string);
}

world runtime {
    import processes;
    import logging;
}
