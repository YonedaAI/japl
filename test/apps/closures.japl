fn make_adder(n: Int) -> fn(Int) -> Int {
  fn(x: Int) { x + n }
}

fn main() {
  let add5 = make_adder(5)
  let add10 = make_adder(10)
  println(show(add5(3)))
  println(show(add10(3)))
  println(show(add5(add10(1))))
}
