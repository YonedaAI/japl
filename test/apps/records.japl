type User = { name: String, age: Int, active: Bool }

fn main() {
  let user = { name: "Alice", age: 30, active: True }
  println(user.name)
  println(show(user.age))
  let updated = { user | age: 31, active: False }
  println(show(updated.age))
  println(show(updated.active))
}
