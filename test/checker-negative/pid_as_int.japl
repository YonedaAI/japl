// Negative test: assigning spawn result (Pid) to an Int-annotated variable must fail.
// Pid and Int are fully distinct types — no implicit conversion.

fn main() {
  let x: Int = spawn(fn() {})
  println(show(x))
}
