type Msg = | Ping(Int) | Pong(Int)

fn worker() -> IO Unit {
  receive {
    Ping(n) => println("got: " <> show(n))
  }
}

fn main() {
  let pid = spawn(fn() { worker() })
  send(pid, Ping(42))
  println("sent")
}
