type Msg = | Ping(Int)

fn worker() -> IO Unit {
  receive {
    Ping(n) => println("remote got: " <> show(n))
  }
}

fn main() {
  let pid = spawn(fn() { worker() })
  send(pid, Ping(42))
  println("sent to worker")
}
